import 'package:flutter/widgets.dart';

// Stub para plataformas no-web (móvil): nunca se usa porque en móvil va Agora
// nativo, pero permite el import condicional sin romper la compilación.
Widget buildJitsiWebView(String room, String displayName) => const SizedBox.shrink();
